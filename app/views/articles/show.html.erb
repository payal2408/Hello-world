<h1><%= @article.title %></h1>

<p><%= @article.body %></p>
<p><%= image_tag (@article.avatar_url) %></p>

<%= link_to "Edit", edit_article_path %>
<%= link_to "Delete", article_path,
	method: :delete,
	data: {confirm: "Are you sure"} %>
	<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>