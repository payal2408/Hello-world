<%= form_with(model: use) do |form| %>
  <% if use.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(use.errors.count, "error") %> prohibited this use from being saved:</h2>

      <ul>
        <% use.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
